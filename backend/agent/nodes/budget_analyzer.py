"""budget_analysis node — deterministic budget checks."""

from agent.state import TripState


def budget_analysis(state: TripState) -> dict:
    """Analyze spend vs. budget without LLM."""
    return {}
