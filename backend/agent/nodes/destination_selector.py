"""select_destination node — LLM-assisted destination selection."""

from agent.state import TripState


def select_destination(state: TripState) -> dict:
    """Choose a destination given preferences and constraints."""
    return {}
