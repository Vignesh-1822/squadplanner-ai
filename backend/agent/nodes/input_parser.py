"""parse_input node — validate and normalize user input."""

from agent.state import TripState


def parse_input(state: TripState) -> dict:
    """Parse natural language / structured input into consensus fields."""
    return {}
