"""search_hotel node — hotel search via tools.serpapi."""

from agent.state import TripState


def search_hotel(state: TripState) -> dict:
    """Run hotel search and attach results to state."""
    return {}
